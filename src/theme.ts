import { DefaultTheme } from "styled-components";

const myTheme: DefaultTheme = {
  borderRadius: "5px",
  colors: {
    main: "#562ECD",
    secondary: "#efebff",
    grey: "#7f8c8d",
    blue: "#3498db"
  }
};

export default myTheme;
