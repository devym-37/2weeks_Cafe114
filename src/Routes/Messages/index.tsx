import React from "react";
import io from "socket.io-client";

//@ts-ignore
// const socket = io("http://localhost");
// function sendMessage(message: string) {
//   socket.emit("newMessage", { message });
// }
const R = () => <span>Messages</span>;

export default R;
